version: '3.8'

services:
  ml-bootcamp-portal:
    build: .
    container_name: ml-bootcamp-portal
    ports:
      - "8585:8585"
    environment:
      - COURSES_DIR=/app/Courses
      - PORT=8585
    restart: unless-stopped
